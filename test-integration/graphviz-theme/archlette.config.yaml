# Integration Test: Graphviz Layout with Custom Theme
#
# This test combines Graphviz auto-layout with a custom theme.
# Runs against the archlette codebase itself for realistic output.

project:
  name: Archlette
  description: Architecture-as-Code toolkit for generating C4 diagrams

paths:
  output: ./output
  ir_out: ./output/ir.json
  dsl_out: ./output/workspace.dsl
  render_out: ./output/diagrams
  docs_out: ./output/docs

extractors:
  - use: extractors/builtin/basic-node
    inputs:
      include: ['../../src/**/*.ts']
      exclude: ['**/*.test.ts', '**/*.d.ts']

validators:
  - use: validators/builtin/base-validator

generators:
  - use: generators/builtin/structurizr
    inputs:
      graphvizLayout: true
      theme: ./custom-theme.dsl

# Direct rendering - structurizr-render now handles Graphviz pre-processing
# Theme is already in DSL from generator, no need to override
renderers:
  - use: renderers/builtin/structurizr-render
    inputs:
      format: png

docs:
  - use: docs/builtin/markdown-docs
